# 内容脚本总览

## 快速创建 frontmatter

每次需要写 Blog 或 Diary，只需运行：

```bash
python3 scripts/create_entry.py --type blog   # 或 diary
```

脚本会：

- 询问标题、摘要（blog）、标签、天气、心情、评分等必要字段
- 自动生成合规的 frontmatter（blog: `title/date/summary/tags/weather/mood/rating/draft`，diary: `date/title/mood/rating/weather/tags`）
- 依据标题或时间戳生成文件名，并写入 `src/content/blog|diary`
- 生成后的文件直接写正文即可，省去手工编辑 frontmatter 的繁琐

常用参数：

```bash
python3 scripts/create_entry.py --type blog --title "训练随记" --summary "今日调参记录" --tags 工程,调参 --weather ☁️ --mood 😌

python3 scripts/create_entry.py --type diary --title "午后感想" --weather 🌤️ --mood 😊 --rating 4
```

## 日记时间戳更新脚本

## 使用方法

### 每次写完日记后：

1. **写完你的日记** - 在 `src/content/diary/` 目录下创建或编辑任何 `.md` 文件

2. **运行脚本**：
   ```bash
   python3 scripts/update_diary_dates.py
   ```

3. **提交并部署**：
   ```bash
   git add .
   git commit -m "feat: add new diary entry"
   git push origin main
   ```

## 脚本功能

- 🔍 自动扫描 `src/content/diary/` 目录下的所有 `.md` 和 `.markdown` 文件
- ⏰ 获取每个文件的真实创建时间
- 📝 为缺少 `date` 字段的文件自动添加创建时间到 front matter
- ✅ 跳过已有 `date` 字段的文件
- 🎯 解决 GitHub Pages 上文件时间被 git 提交时间覆盖的问题

## 输出示例

```
✅ 成功更新了 2 个文件：

  📝 今天的工作总结.md
     → date: 2025-11-02 14:30:22

  📝 周末计划.md
     → date: 2025-11-01 09:15:10
```

## 注意事项

- 脚本会自动检测文件的创建时间（`birthtime`），优先使用文件系统记录的实际创建时间
- 如果文件已有 `date` 字段，脚本不会覆盖
- 生成的 front matter 格式：
  ```yaml
  ---
  date: "2025-11-02 14:30:22"
  ---
  ```
